#!/usr/bin/env node

/**
 * Sincronizador de Horários — DESATIVADO
 * 
 * Este script foi desativado porque o usuário quer gerenciar os dados manualmente pelo Supabase.
 * Para reativar, restaure o conteúdo original ou remova o return abaixo.
 */

console.log('⏸️ Sincronizador de horários desativado.')
console.log('Os dados dos matches agora são gerenciados manualmente pelo Supabase.')
process.exit(0)